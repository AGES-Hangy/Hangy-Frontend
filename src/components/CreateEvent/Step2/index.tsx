import { View } from 'react-native';

/**
 * Stub da etapa 2 do wizard. O conteúdo (data, horário, local, limite de
 * participantes, privacidade e a publicação em si) é da task 094 — aqui fica
 * só o alvo da navegação do botão "Continuar" da etapa 1.
 */
export function Step2() {
  return <View />;
}
